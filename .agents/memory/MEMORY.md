# Memory index

- [Integration settings (control panel)](integration-settings.md) — superadmin sets DeepSeek/Resend/JaaS keys in-app; secrets are write-only; JaaS creds resolve as an all-or-nothing DB-or-env triplet.
- [Self-hosting on bare Ubuntu](self-hosting.md) — local storage driver, idempotent installer, PWA at /app, and the "/app 404" recovery when the server doesn't know its own domain (DB-stored panel domain vs .env).
