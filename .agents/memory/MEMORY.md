- [Jefe de Departamento model](jefe-departamento-model.md) — ADG "Jefe de Departamento ADG por centro" is the `department_head` ROLE scoped to a center, NOT a departments entity.
- [Tool-output display redaction](tooling-display-redaction.md) — some tokens render as `ln` in read/rg/bash output though file bytes are fine; trust `rg -c` counts and `tsc`, not rendered text.
- [Invitation token lifecycle](invitation-token-lifecycle.md) — codes generated by role only (email supplied at register); renew must reject used/revoked; consume atomically with row lock.
- [Self-service profile editing](self-service-profile.md) — users edit own name/email/password via PATCH /auth/me (requireAuth), NOT admin PATCH /users/{id} which blocks self via canManageUser.
